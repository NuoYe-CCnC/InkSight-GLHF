# 验收与候选包

日期：2026-09-14

## 必测行为

- 新安装六个来源全部关闭。
- 旧 `sources` 选择迁移为等价开关，显式新开关不被旧字段覆盖。
- 全部关闭时 RSS 网络请求为 0，新闻模型调用为 0，显示稳定的本地每日寄语。
- 缺少密钥时先本地回退，不采集、不调用模型。
- 旧缓存和手工传入候选在提示词前再次按允许来源过滤。
- 空候选不回读全量缓存，不伪报新摘要发布成功。
- Web 只暴露来源、链接、标题和报道时间等必要元数据；墨水屏布局不变。

## 结果

- 后端全量回归：842 项通过，0 项失败；2 条为既有运行库环境/弃用警告。
- 候选允许列表源文件：293；压缩包文件：297。
- 全新解压复核：路径穿越 0、重复路径 0、符号链接 0、嵌套归档 0，文件树匹配。
- 隐私与内容扫描：发现 0；候选内生产配置、私密配置、运行状态、字体、PNG/JPG 均为 0。
- 连续两次构建的 ZIP SHA-256 相同，证明当前输入下确定性重建通过。
- 最终 ZIP SHA-256 写在候选目录外的 `release-candidate/InkSight-Source.zip.sha256` 和 `.receipt.json`，避免在 ZIP 内形成自引用哈希。
- `publication_ready=false`；当前只剩源码包内容权利人确认门禁，远程新闻源默认策略和工程验证不再作为未决项。
