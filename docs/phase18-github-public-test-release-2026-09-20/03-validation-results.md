# 阶段 18 发布前验证结果

日期：2026-09-20（Asia/Shanghai）

- 全量自动回归：873 项通过，0 项失败；2 条为既有 LibreSSL/urllib3 与 DashScope 弃用警告。
- 阶段 11 至 18 文档/发布专项：56 项通过。
- 候选 ZIP 在一次性全新目录解压后，随包阶段 11 至 18 专项：56 项通过。
- 候选构建器两次重建的 ZIP SHA-256 与候选树 SHA-256 分别一致。
- 构建器对最终候选执行逐文件清单、危险路径、重复路径、符号链接、嵌套归档、隐私模式、字体、固件/预览二进制和全新解压一致性检查。
- 本次按发布者要求跳过从公开 GitHub 下载后的安装与运行验收；远程只进行页面、链接、标签、Release 资产与安全入口的轻量核对。
- 没有重启生产服务、烧录设备或调用真实付费 API。

ZIP 和候选树的最终摘要保存在候选目录外的 `.sha256` 与 `.receipt.json` 中，不写回归档，避免自引用摘要。
