# 2026-09-24 Token 显示与误切页修复记录

## 结论

- `AI 用量`页继续使用 k/M/B 紧凑格式：`0–999` 为整数，`1k–9.99k` 保留两位小数，`10k–99.9k` 保留一位，`100k–999k` 不保留小数；M/B 同理。
- `今日关注`页显示今日 Token 的完整整数，`0` 与未知值 `--` 保持不同语义。
- 最近一小时的反复切页不是 DeepSeek 新闻扣费归因失败，而是同一 Codex 重置窗口的时间戳在相邻秒之间抖动，被误认为新的活动。

## 现场证据

观察窗口为 2026-09-24 15:15–16:15（Asia/Shanghai）。

- DeepSeek 原始余额与归因后的有效余额均保持不变；没有待结算或临时扣费事件。公开记录不包含账户实际金额。
- 新闻与金价版本基本稳定，AI 版本在多个采样点之间来回变化。
- 两次连续采样中，Codex 已用百分比、点数、到期信息、DeepSeek 余额和 Token 均相同，只有重置时间相差 1 秒；活动键随之变化。
- 这说明误切页来自 `codex_resets_at` 的秒级噪声，不是 DeepSeek 定时新闻调用。真实 Codex 百分比上升仍属于应当触发的活动。

## 修复

后端在活动指纹、AI 视觉指纹、资讯视觉指纹和 AI 传输版本中，将 Codex 重置时间归一到面板实际显示的分钟。原始载荷中的精确时间不修改；真实百分比、余额、点数、到期列表与跨分钟窗口变化仍参与判断。

固件和本地预览分别选择格式：

- `AI 用量`：紧凑 Token。
- `今日关注`：完整整数 Token。

## 验证

- 后端完整测试：886 项通过。
- 新增回归：相差 1 秒的同一重置窗口不会改变三类活动/视觉键；超过一分钟的变化仍会改变。
- 版面边界：`0`、`999`、`1000`、`10000`、`99999`、`999999999` 和 64 位有符号整数最大值均不会与相邻金额重叠。
- 固件目标 `epd_426_ssd1677_s3_n32r16` 编译成功；RAM 29.5%，Flash 67.7%。
- 发布候选会独立生成并执行隐私扫描；公开提交记录见 [GitHub main 提交历史](https://github.com/NuoYe-CCnC/InkSight-GLHF/commits/main)。

## 部署与回退

- 生产后端已重启应用修复；重启前备份位于本机 `2026-09-24-token-page-activity` 备份目录，并带 SHA-256 清单。
- 固件源码已构建验证，但本次没有刷写设备。实体屏上的完整整数 Token 需要以后明确授权刷写才会生效。
- 如需回退，停止后端后恢复备份中的对应源码与状态文件，再重新启动；固件未刷写，因此不存在本次设备端回退。

## 后续观察

部署后继续自然观察一次以上的 Codex 推送周期。预期秒级重置时间抖动不再触发切页，而真实用量变化仍正常切换；不为验收主动调用付费模型。
