# GPL-3.0-only 开源候选收尾实施计划

日期：2026-09-14

## 已确认范围

- 顶层项目许可证采用 `GPL-3.0-only`，不写成 `GPL-3.0-or-later`。
- 许可证选择只解决发布者对其有权授权内容的许可表达，不替代作者、贡献者、第三方资源或改编来源证明。
- 不创建或发布 GitHub 仓库，不烧录设备，不重启生产后台，不更改现有账号和密钥，不调用付费模型、新闻接口或云端写入。
- 私密配置、数据库、字体、固件产物、缓存和生产状态保持原样，不进入公开候选。

## 实施顺序

1. 加入未经改写的 GNU GPL v3 正文作为顶层 `LICENSE`，增加项目许可范围说明；保留现有第三方文件头、MIT/BSD/Apache/LGPL/GPL 文本和通知。
2. 只读比对 InkSight、Waveshare 等权威上游，记录可精确证明的文件级对应、版本/提交和许可；推测内容单独标记，不伪造 Git 历史。
3. 为 Python 3.9 的实际支持基线生成完整、带哈希锁文件，在 ZIP 全新解压副本和新虚拟环境中验证安装；其他平台和 Python 版本明确标为未验证。
4. 使用官方页面核对 OpenAI、DeepSeek、新闻来源、XAUS/gold-api.com 与坚果云 WebDAV 的配置、数据、展示和条款边界；只记录证据与工程限制，不代替用户接受条款或作法律结论。
5. 更新 README、SBOM、第三方通知、资源清单、支持矩阵和发布门禁；保持 MiSans 本地导入且不分发原始或派生产物。
6. 从允许列表重建候选并连续构建两次；对实际 ZIP 全新解压树运行隐私/归档检查、专项测试、全量回归、离线启动、管理端安全、字体链路和首个已支持固件环境构建。
7. 输出本阶段实施修改、来源证据、许可范围、哈希锁安装结果、服务条款矩阵、候选路径/SHA、门禁表和用户最小确认清单，并同步回来源任务。

## 停止与询问条件

- 若清除来源疑问必须删除现有硬件目标、重写驱动、接受新服务条款、公开上传或触碰生产环境，则暂停对应部分并请用户决定。
- 自动化检查通过不等于证明全部权属或法律合规；未取得证据的项目继续留在发布门禁中。
