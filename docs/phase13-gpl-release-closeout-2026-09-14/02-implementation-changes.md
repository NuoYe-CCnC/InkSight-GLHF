# 实施修改

日期：2026-09-14

本阶段只处理开源候选的许可、来源、依赖复现、服务边界、说明和验收，不触碰生产配置、设备、真实账号或 GitHub。

## 已完成

- 顶层加入未经改写的 GNU GPL v3 正文 `LICENSE`，项目许可证表达固定为 `GPL-3.0-only`。
- 加入 `LICENSE-SCOPE.md`，明确项目许可不覆盖第三方文件、字体、商标、数据和外部服务条款。
- 新增 `requirements-py39-macos-arm64.lock`，为 Python 3.9 / macOS ARM64 提供完整版本及 SHA-256 哈希。
- 修复干净环境暴露的隐式日期依赖：用可在 macOS ARM64 安装的 `lunar-python==1.4.8` 替代缺少可用制品的 sxtwl 2.0.7，并保持原有农历、节气和节日测试结果。
- 保留 `requirements.lock.txt` 作为本机审计快照，不再把它描述成可复现锁文件。
- 更新 `SBOM.json`、`THIRD_PARTY_NOTICES.md`、README 和候选构建器。
- 候选包继续生成 `PUBLICATION-BLOCKED.md`；顶层许可证缺失不再是阻塞项，未确认的后续代码权属、CC0 资格和新闻源条款仍是阻塞项。
- 候选允许列表纳入本阶段文档、许可证范围说明、哈希锁文件和专项测试。

## 明确未做

- 未创建 Git 仓库、提交、推送或发布 Release。
- 未连接、烧录或重启设备，未修改 Wi-Fi、坚果云、DeepSeek、OpenAI 等真实配置。
- 未调用付费 API、新闻接口或云端写入。
- 未把 MiSans 字体、生成字体头、固件二进制、数据库、日志、缓存、私密配置或来源未明图片放进候选包。

## 解释

选择 GPL-3.0-only 是必要的许可证决策，但不能自动证明发布者拥有所有新增代码和文案，也不能替代新闻源或 API 服务的使用条款。候选包能通过技术验收，仍可能维持“禁止发布”。
